"""
Purpose:
    Expose the concrete browser implementations.

Responsibilities:
    - Root of concrete adapter and wrapper implementations (e.g. Playwright).

Must NOT do:
    - Expose direct Playwright libraries to the high-level layers.
"""
